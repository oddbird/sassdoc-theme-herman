machine:
  node:
    version: 7.10.0

dependencies:
  override:
    - yarn

test:
  override:
    - nvm use 4.0.0 && yarn test
    - nvm use 7.10.0 && yarn test
    - ./node_modules/.bin/gulp eslint
    - ./node_modules/.bin/gulp sasslint
