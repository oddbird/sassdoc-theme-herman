machine:
  node:
    version: 6.9.1

dependencies:
  override:
    - npm install

test:
  override:
    - nvm use 4.0.0 && npm test
    - nvm use 6.9.1 && npm test
    - ./node_modules/.bin/gulp eslint
    - ./node_modules/.bin/gulp sasslint
