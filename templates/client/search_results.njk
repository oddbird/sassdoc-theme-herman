<h1 class="search-heading">
  <span class="search-term">{{ term }}</span>:
  {{ count or 'No' }} {{ 'result' if count == 1 else 'results' }}
</h1>

{% if count %}
  <ul class="js-search-results search-results"></ul>
{% endif %}
