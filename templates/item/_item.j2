{% import 'item/macros.j2' as show %}
{% import 'item/annotation.macros.j2' as annotation %}
{% import 'item/example.macros.j2' as example %}
{% import 'item/preview.macros.j2' as preview %}

{% set slug = item.context.name|urlencode|replace("'", '') %}
{% set id = [type, slug]|join('--') %}
{% set display_private = display and display.access and ('private' in display.access) %}

<section class="item" id="{{ id }}">
  {{ show.heading(item, type, id, display) }}

  {{ show.source(item, type) }}
  {{ show.summary(item.description) }}

  {{ annotation.param_or_prop(
    params_data=item.parameter,
    props_data=item.property,
    content_data=item.content,
    return_data=item.return,
    output_data=item.output
    ) }}

  {{ annotation.related(item, display_private) }}
  {{ annotation.require_or_used(item, display_private) }}
  {{ annotation.todo(item) }}

  {{ example.list(item.example, id) }}

  {{ preview.icons(item.icons) }}
  {{ preview.show_previews(
    data=sassjson,
    name=item.context.name,
    args=item.preview
  ) }}
</section>
